# manifest.yml for Netlify deployment

applications:
  - name: admin-panel-autoglass-shop
    build:
      command: npm run build
      publish: dist
    environment:
      NODE_VERSION: 20
    routes:
      - path: "/*"
        serve: "/index.html"